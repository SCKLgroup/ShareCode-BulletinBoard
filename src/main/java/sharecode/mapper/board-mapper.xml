<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<select id="boardAll" resultType="sharecode.vo.PostVO">
		SELECT post_no, post_title, post_content, post_category,
		post_writer,post_date,post_hit
		FROM POST
		<include refid="search"></include>
		ORDER BY post_no desc ,post_date desc
	</select>

	<select id="countArticle" resultType="int">
		SELECT COUNT(*) FROM POST
		<include refid="search"></include>
	</select>

	<sql id="search">
		<choose>
			<when test="searchOption == 'all'">
				WHERE post_writer like '%'||#{keyword||'%'
				OR post_content like '%'||{#keyword}||'%'
				OR post_title like '%'||{#keyword}||'%'
				OR post_category like '%'||{#keyword}||'%'
			</when>
			<when test="searchOption == 'Java'">
				WHERE post_writer like '%'||#{keyword||'%'
				OR post_content like '%'||{#keyword}||'%'
				OR post_title like '%'||{#keyword}||'%'
			</when>
			<when test="searchOption == 'Python'">
				WHERE post_writer like '%'||#{keyword||'%'
				OR post_content like '%'||{#keyword}||'%'
				OR post_title like '%'||{#keyword}||'%'
			</when>
			<when test="searchOption == 'C++'">
				WHERE post_writer like '%'||#{keyword||'%'
				OR post_content like '%'||{#keyword}||'%'
				OR post_title like '%'||{#keyword}||'%'
			</when>
			
			<otherwise>
				where ${searchOption} like '%'||{#keyword}||'%'
			</otherwise>
		</choose>
	</sql>
</mapper>